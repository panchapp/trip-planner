{
  "name": "trip-planner-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Trip Planner",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "start:client": "pnpm --filter app-client start",
    "start:service": "pnpm --filter app-service start:dev",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7"
  }
}
